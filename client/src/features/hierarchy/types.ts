export interface Hierarchy {
  id: number;
  json: string;
}
export interface VisProps {
  canvasHeight: number;
  canvasWidth: number;
  divId: number;
  margin?: any; //(_:any):void
  render: any; //(_:any):void
}
